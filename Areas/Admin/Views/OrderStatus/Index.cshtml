@model IEnumerable<ProjectShopIdentity.Areas.Admin.Models.Order_Status>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderSName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderSName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderSID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderSID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderSID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
